Hola Maximiliano! 



Muchas gracias por la entrega! 



Está bastante bien, es funcional.
La configuración tiene su propia carpeta.
El Dao está separado de la app
Los middlewares están bien.




Te está faltando separar en controllers. 



En las rutas, como por ejemplo:



cartRouter.post('/api/carts', async (req, res) => {

    try {

        const cartId = await cartManager.addCart();

        res.status(201).send({ message: 'Carrito creado exitosamente.', cartId });

    } catch (error) {

        console.error('Error:', error.message);

        res.status(500).send('Internal Server Error');

    }

});



Tenés acoplado el 'controlador', lo que debería quedar es así;



cartRouter.post('/api/carts', cartController.addCart )





toda la lógica la va a tener el "addCart" de cartController.  



Tenés que crear una carpeta controllers, y crear una clase CartController y que tenga en sus metodos el addCart o hacer una función.



addCart =  async (req, res) =>{

 try {

        const cartId = await cartManager.addCart();

        res.status(201).send({ message: 'Carrito creado exitosamente.', cartId });

    } catch (error) {

        console.error('Error:', error.message);

        res.status(500).send('Internal Server Error');

    }

}





Cualquier consulta que tengas avisame! 



saludos! 